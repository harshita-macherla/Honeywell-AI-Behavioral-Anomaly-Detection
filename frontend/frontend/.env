# Base URL of the FastAPI backend (backend/app/main.py). The backend must
# be running (uvicorn backend.app.main:app --port 8000) for this dashboard
# to load any data -- see README.md.
VITE_API_BASE_URL=http://127.0.0.1:8000
